{
  "name": "read-and-write",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build:web": "cd frontend && npm install --no-audit --no-fund && npm run build",
    "deploy": "npm run build:web && wrangler d1 migrations apply read-and-write --remote && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "db:local": "wrangler d1 migrations apply read-and-write --local",
    "db:remote": "wrangler d1 migrations apply read-and-write --remote"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
